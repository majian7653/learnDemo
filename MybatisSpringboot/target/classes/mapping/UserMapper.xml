<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="org.example.entity.User">
        <result column="id" jdbcType="INTEGER" property="id"/>
        <result column="userName" jdbcType="VARCHAR" property="userName"/>
        <result column="passWord" jdbcType="VARCHAR" property="passWord"/>
        <result column="realName" jdbcType="VARCHAR" property="realName"/>
    </resultMap>

    <sql id="Base_Column_List">
    ID,USERNAME,PASSWORD,REALNAME
    </sql>

    <insert id="insert" parameterType="org.example.entity.User" useGeneratedKeys="true" keyProperty="id">
        insert into user (ID,USERNAME,PASSWORD,REALNAME) values
        (#{id},#{userName},#{passWord},#{realName})
    </insert>

    <delete id="deleteById" parameterType="org.example.entity.User">
        delete from user where id = #{id}
    </delete>

    <select id="selectById" resultType="org.example.entity.User">
        select * from user where id = #{id}
    </select>

    <select id="getAllUser" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user
    </select>









</mapper>